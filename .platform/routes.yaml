"https://{default}/":
    type: upstream
    upstream: "cigionline:http"

"https://www.{default}/":
    type: redirect
    to: "https://{default}/"

"https://{default}/static/":
    type: proxy
    to: "https://{variables.env:STATIC_PROXY}/static/"